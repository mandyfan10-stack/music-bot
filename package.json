{
  "name": "xxii-sound-music-bot",
  "version": "1.0.0",
  "private": true,
  "description": "XXII SOUND — Telegram Mini App with Supabase backend",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i src/tailwind.input.css -o src/tailwind.css --minify",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "tailwindcss": "3.4.19"
  }
}
