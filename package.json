{
  "name": "xxii-sound-music-bot",
  "version": "1.0.0",
  "private": true,
  "description": "XXII SOUND — Telegram Mini App (frontend)",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i src/tailwind.input.css -o src/tailwind.css --minify",
    "test": "node --test"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
